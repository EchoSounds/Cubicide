using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestButtonScript : ButtonInput
{
    // Start is called before the first frame update
    void Start()
    {
        InstMultiButton();
    }

    // Update is called once per frame
    void Update()
    {
    }
}
